import { StyleSheet, Text, View } from "react-native";
import React from "react";
import CreateFlatList from "../toolBuild/CreateFlatList";

const NewsScreen = () => {
  return <CreateFlatList />;
};

export default NewsScreen;

const styles = StyleSheet.create({});
